import pytest
from pig_code_110718_3pm import Game, Dice, Score

def test_create_roll():
    # assert dice.roll = int(4)
    # assert dice.roll = int(1)
    # looking for valid print/outcome from a roll
    # rest of function here

def test_computer_roll():
    # assert dice.roll = int(4)
    # assert dice.roll = int(1)
    # looking for valid print/outcome from a roll
    # rest of function here

def test_dice():
    # dice test function here
    # not sure what to test for dice, other than it represents 1-6, and is random


def test_ask_human_to_roll():
    assert response = "r"
    assert response = "h"
    assert response = "x"
    assert response = "A"


def add_roll_to_human_roll():
    assert self.human_roll = 1
    assert self.human_roll = 2
    assert self.human_roll


def test_roll_has_a_score():
    self.human_roll = 2
    assert dice.get_score() = 2  
    # DOUBLE CHECK THAT CLASS/ATTRIBUTE/METHOD NAMING IS CORRECT

def test_roll_to_str():
    self.human_roll = 2
    assert str(roll) == "You rolled a 2"


def test_roll_to_str_bust():
    self.human_roll = 1
    assert str(roll) == "You busted!"
    # DOUBLE CHECK THAT THIS STRING MATCHES WITH THE PIG.PY FILE'S STRING


def test_add_roll_to_human_roll():
    self.human_roll(4) + self.human_roll(2)
    self.human_roll = 0
    self.human_roll + self.human_roll
    # This should also correlate to the SCORE class like so:
    # total_score = 0
    # total_score = total_score + get_turn_score()


def test_dice_has_6_values():
    dice = Dice()
    assert len(Dice) == 6
    # Not sure if this should be in a list?


def test_dice_has_6_values():
    assert roll(4) in dice.roll
    # Again - not sure how to present this at this time

def test_start_game():
    # need to test to see if the greater roll goes first


